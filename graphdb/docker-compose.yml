
# http://localhost/askomics/
# http://localhost/virtuoso

version: '2'
services:
    askomics:
        image: askomics/askomics
        command: ./startAskomics.sh -t graphdb -d prod
        environment:
            ASKO_endpoint: "http://graphdb:7200/repositories/SYSTEM"
            ASKO_updatepoint: "http://graphdb:7200/repositories/SYSTEM/statements"
            ASKO_load_url: "http://askomics:6543"
            ASKO_smtp_host: postfix
            ASKO_smtp_port: 25
            ASKO_smtp_login: admin
            ASKO_smtp_password: passasko
            ASKO_endpoint_username : dba
            ASKO_endpoint_passwd : dba
        volumes:
            - ./output-container/askomics/ttl/askomics:/tmp

    graphdb:
        image: ontotext/graphdb:8.3.0-se
        volumes:
            - ./output-container/graphdb:/opt/graphdb/home
    nginx:
        image: nginx
        volumes:
            - ./mysite.template:/etc/nginx/conf.d/mysite.template
            - ./output-container/nginx/log:/var/log/nginx/
        ports:
            - "80:80"
        environment:
            NGINX_HOST: localhost
            NGINX_PORT: 80
            ASKOMICS_REL_URL: askomics #=> http://localhost:80/askomics
            TPS_REL_URL: graphdb      #=> http://localhost:80/graphdb
        command: /bin/bash -c "envsubst < /etc/nginx/conf.d/mysite.template > /etc/nginx/conf.d/default.conf && nginx -g 'daemon off;'"

    postfix:
        image: catatnight/postfix
        environment:
            maildomain: mail.askomics.org
            smtp_user: admin:passasko
